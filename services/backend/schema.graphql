type demo {
  id: String!
  version: String!
}
type Query {
  getDemo(id: String!): demo
  getDemos: [demo!]
}
input DemoInput {
  version: String!
}
type Mutation {
  createDemo(input: DemoInput!): demo
}
